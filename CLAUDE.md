## Agent skills

### Issue tracker

Issues live in GitHub Issues for f3r21/product-framing-skills, handled with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five default triage roles, each label named after its role. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` and `docs/adr/` at the repo root, created when a term or a decision is settled. See `docs/agents/domain.md`.
